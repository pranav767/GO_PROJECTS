# Movie Reservation System Environment Configuration

# JWT Secret Key (change this in production!)
HMAC_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=adminpass
DB_NAME=movie_reservation

# Server Configuration
PORT=8080
GIN_MODE=debug

# Application Configuration
SEAT_LOCK_DURATION=5m
MAX_SEATS_PER_RESERVATION=8
CANCELLATION_WINDOW=2h
# Dynamic booking/cancel windows (overrides defaults 30m / 2h if set)
BOOKING_CUTOFF_MINUTES=30
CANCEL_CUTOFF_HOURS=2

# Security Configuration
BCRYPT_COST=10
JWT_EXPIRY=24h

# Admin Management
# Optionally override / reset the default admin password at startup.
# If set, the existing 'admin' user's password will be updated to this value.
ADMIN_DEFAULT_PASSWORD=admin123


# Development Convenience
# If set to true/1/yes, all data tables are truncated on startup BEFORE schema init & seeding.
# Use ONLY in local/dev environments. Leaves schema intact but wipes data and reseeds demo/test data.
# Recommended for development: DB_RESET_ON_START=true
# See README for details.
DB_RESET_ON_START=true

# Baseline showtime generation
# Start generating baseline showtimes this many days from today (1 = tomorrow only by default)
BASELINE_SHOWTIME_START_OFFSET_DAYS=1
# Number of consecutive days (starting at offset) to ensure baseline showtimes exist
BASELINE_SHOWTIME_DAYS=2